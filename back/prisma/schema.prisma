generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Cliente {
  id            Int    @id @default(autoincrement())
  cliente       String
  transcripcion String
  escenario     String
  linkVideo     String
  preguntas     String

  @@map("clientes")
}
